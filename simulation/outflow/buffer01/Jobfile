# Configuration file for `jobrunner`

job.input = ["flash.par"]

# list/patterns of files to
# archive and store in the
# directory tree
job.archive = ["INS*"]

# schedular options
schedular.options = ["#SBATCH --job-name=outbuf01"]
