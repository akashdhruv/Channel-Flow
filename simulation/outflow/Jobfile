# Configuration file for `jobrunner`

# target file for
# runnning simulations
job.target = "Maplefile"

job.submit = ["outflow.sh"]

# Schedular configuration resources requirement
# job name etc
schedular.options = [
    "#SBATCH --ntasks=5",
    "#SBATCH -t 0-30:00",
]
