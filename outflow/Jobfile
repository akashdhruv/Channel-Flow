# Configuration file for `jobrunner`

# target script for
# runnning simulations
config.target = "outflow.sh"

# Schedular configuration resources requirement
# job name etc
schedular.options = [
    "#SBATCH --ntasks=5",
    "#SBATCH -t 0-30:00",
]
